import React from "react";

const CommonHeadings = ({ title }) => {
  return (
    <h2 className="uppercase text-4xl font-bold  text-center w-full tracking-widest text-dark">
      {title}
    </h2>
  );
};

export default CommonHeadings;
